namespace Retard.Core.Models
{
    /// <summary>
    /// Permet de partager des données entre les scènes.
    /// Garde l'état de la session (pause, chargement de données, etc.)
    /// </summary>
    public static class GameState
    {
    }
}
